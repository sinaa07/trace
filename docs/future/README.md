# Advanced / Future

This directory is a parking area for functionality scheduled after the MVP/Core evidence foundation. Keep advanced agents, causal or temporal engines, and other later-phase capabilities out of `backend/app/core` and `frontend/src/core` until their contracts and phase are approved.
